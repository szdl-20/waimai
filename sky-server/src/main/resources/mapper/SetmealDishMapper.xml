<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">



<select id = "getSetmealIdByDishid" resultType = "Long">
select s.setmeal_id from setmeal_dish s where s.dish_id in

<foreach collection = "dishIds" item =  "df" separator="," open="(" close=")">
 #{df}

</foreach>

</select>




        <!-- insertBatch --> 

        <insert id="insertBatch" parameterType = "List" >

        insert into setmeal_dish( setmeal_id,dish_id,name,price,copies) values
        <foreach collection="setmealDishs" item="sd" separator=",">
        (#{sd.setmealId},#{sd.dishId},#{sd.name},#{sd.price},#{sd.copies})
        </foreach>


        </insert>
        
        




        <!-- deleteBatch --> 

        <delete id="deleteBatch" parameterType = "List">
        delete from setmeal_dish s where s.setmeal_id in 
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
        #{id}


        </foreach>


        </delete>        
        </mapper>
