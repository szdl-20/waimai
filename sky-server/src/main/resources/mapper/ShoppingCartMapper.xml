<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">




    <!-- select --> 

    <select id="select" resultType="ShoppingCart" parameterType="ShoppingCart">
    
    select * from shopping_cart as s 
    
    <where>
    <if test="userId != null">
    and  s.user_id= #{userId}
    </if>

       <if test = "dishId != null"> and 

    s.dish_id = #{dishId}
    </if>
    <if test = "setmealId != null"> and s.setmeal_id = #{setmealId } </if>
<if test = "dishFlavor != null"> and s.dish_flavor = #{dishFlavor} </if>

   
 
    </where> 
    
    


    </select>    



    <insert id="insert" parameterType="ShoppingCart">
    insert into shopping_cart(name,user_id,dish_id,setmeal_id,dish_flavor,number,amount,image,create_time)
    values
    (#{name},#{userId},#{dishId},#{setmealId},#{dishFlavor},#{number},#{amount},#{image},#{createTime})


    </insert>    
    
          </mapper>
